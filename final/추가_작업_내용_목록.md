**추가로 작업할 내용 목록 (Additional Work Items List)**

**1. 콘텐츠 추천 (Content Recommendation)**
    *   **Kollus 시청 기록 연동:** `StudentContentRecommendService.java`의 `fetchLessonStudyStatus` 메서드를 실제 Kollus 시청 기록 또는 유사한 메커니즘과 연동하도록 수정.
    *   **추천 중복 제거 활성화:** `StudentContentRecommendService.java`의 `ENABLE_RECO_DEDUPE` 플래그를 `true`로 설정.
    *   **`LM_LESSON.ID` (INT)와 `TB_RECO_CONTENT.lesson_id` (VARCHAR/Kollus Key) 연동 전략 명확화:** 프론트엔드에서 `TB_RECO_CONTENT`의 `lessonId` (Kollus 키)를 실제 콘텐츠와 매핑하여 표시하는 방식에 대한 명확한 이해 또는 문서화.
    *   **이중 색인 소스 추천 품질 평가:** AI 요약(`TB_RECO_CONTENT`)과 레거시 설명(`LM_LESSON`)의 이중 색인이 추천 품질에 미치는 영향을 평가하고, 레거시 데이터의 품질이 낮다면 AI 요약 기반의 추천만 활용하는 것을 고려.

**2. 직종 코드 (Occupation Code)**
    *   **JobKorea 직종 코드 Qdrant 색인 구현 검증:** `JobKoreaOccupationVectorIndexService.java`의 구현을 직접 확인하여 Qdrant로의 정확한 색인 방식과 메타데이터 포함 여부를 검증.
    *   **자연어 직무 검색 UI 구현 및 표시:** 프론트엔드에서 자연어 기반의 직무 검색(`JobController`의 `/job/recruits/nl` 엔드포인트)을 위한 사용자 인터페이스 제공 및 AI가 도출한 직무 분류 결과 명확하게 제시하는지 확인.
    *   **`occupationcode` 테이블 데이터 최신화:** `JobService.java`의 `loadOccupationCodesFromCsv()` 메서드가 사용하는 CSV 파일(`work24.occupation-csv-path`)이 항상 최신 Work24 직종 코드로 유지되는지 확인.
    *   **직무 재분류 불일치 로그 검토 및 튜닝:** `JobService.filterJobKoreaByRequestedOccupation`에서 기록되는 "불일치 샘플" 로그를 정기적으로 검토하여 AI 기반의 직무 재분류 정확도 개선.

**3. 폴리텍 관련 데이터 (Polytech-related Data)**
    *   **`StatisticsDashboardPageController.java` 및 `StatisticsDashboardApiController.java` 존재 확인:** 이 컨트롤러 파일들이 초기 탐색 시 발견되지 않았습니다. 통계 대시보드 기능이 어떻게 노출되는지 파악하기 위해 정확한 경로 확인 또는 대체 구현 여부 파악.
    *   **통계 대시보드 프론트엔드 구현 확인:** 백엔드의 통계 서비스를 활용하여 KOSIS 데이터, 학생 통계, 입시 정원, 전공-산업 정렬 등의 정보를 시각화하는 종합적인 프론트엔드 대시보드가 구현되었는지 확인.
    *   **내부 통계 실시간 연동 방안 고려:** 현재 내부 통계(학생 인구, 입시 정원, 취업률 등)는 주로 Excel 파일로부터 로드됩니다. 보다 동적이고 최신 데이터를 반영하는 보고를 위해, 이러한 데이터를 라이브 데이터베이스 또는 데이터 웨어하우스와 연동하는 방안 고려.
    *   **KOSIS 캐시 갱신 메커니즘 구현:** `KosisStatisticsService.java`의 캐시된 KOSIS 데이터를 주기적으로 갱신하는 스케줄러 또는 관리자용 갱신 API 엔드포인트 구현.

**4. 개인 정보 (Personal Information)**
    *   **휴대전화 번호 (`TB_USER.MOBILE`) 암호화:** `TB_USER` 테이블의 `MOBILE` 필드가 현재 평문으로 저장될 가능성이 있으므로, DB 저장 시 암호화(예: AES256)하고 필요 시에만 복호화하는 방식으로 변경 고려.
    *   **본인인증 DI 값 (`TB_USER.DUPINFO`) 처리 강화:** `TB_USER` 테이블의 `DUPINFO` (본인인증DI값) 저장 방식(평문/해시/암호화 여부)을 확인하고, 평문이라면 솔트(salt)를 사용한 강력한 해시 또는 암호화를 적용.
    *   **PII 접근을 위한 세분화된 접근 제어 (RBAC) 강화:** 사용자 유형에 따른 PII 접근에 대한 세분화된 역할 기반 접근 제어(RBAC) 시스템이 견고하게 구현 및 검증.
    *   **데이터 보존 및 익명화 정책 구현:** PII에 대한 명확한 데이터 보존 정책을 정의하고, 보존 기간 만료 시 데이터를 익명화하거나 삭제하는 메커니즘 구현.
    *   **PII 접근 감사 로깅 강화:** `SecureLogger.logDataAccess`가 모든 주요 PII 접근 이벤트에 대해 일관되게 적용되고, 감사 추적을 위한 충분한 정보가 기록되고, 감사 추적을 위한 충분한 정보가 기록되는지 확인.
    *   **모의 침투 테스트 및 보안 감사 수행:** PII 보호와 관련하여, 정기적인 모의 침투 테스트 및 보안 감사를 수행하여 시스템의 취약점을 발견하고 개선.